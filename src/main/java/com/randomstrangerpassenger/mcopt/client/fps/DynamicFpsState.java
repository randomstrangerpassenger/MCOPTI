package com.randomstrangerpassenger.mcopt.client.fps;

/**
 * Represents the current window/activity state for the dynamic FPS controller.
 */
public enum DynamicFpsState {
    IN_GAME,
    IDLE,
    MENU,
    UNFOCUSED,
    MINIMIZED
}
